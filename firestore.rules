rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para Contabilidad del Hogar y Finanzas Personales
    match /{document=**} {
      allow read, write: if true; // Para uso personal directo o configurar con request.auth != null
    }
  }
}
